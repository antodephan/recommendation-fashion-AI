# Sao chép file này thành `.env` và điền giá trị thật (không commit `.env`).

# Bắt buộc để bật GPT (trích ngữ cảnh, phân loại ý định, văn gợi ý).
OPENAI_API_KEY=

# Tuỳ chọn — mặc định gpt-5.4-mini (GPT-5.4 mini trên API).
OPENAI_MODEL=gpt-5.4-mini
